# Workflow

- before updating the `./index.xml` pull changes from main
- before commiting changes update the script versions from the changes scripts

# versions

- version schema: major.minor.patch
- small fixes increments the patch version
- new features increments the minor version
- breaking api changes increments the major version
